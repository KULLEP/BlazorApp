<div class="top-row ps-3 navbar navbar-dark">
    <div class="container-fluid">
        <a class="navbar-brand" href="">BlazorApp</a>
        <button title="Navigation menu" class="navbar-toggler" @onclick="ToggleNavMenu">
            <span class="navbar-toggler-icon"></span>
        </button>
    </div>
</div>

<div class="@NavMenuCssClass" @onclick="ToggleNavMenu">
    <nav class="flex-column">
        <div class="nav-item px-3">
            <NavLink class="nav-link" href="apod" Match="NavLinkMatch.All">
                <span class="oi oi-list-rich" aria-hidden="true"></span> Apod
            </NavLink>
        </div>
        <div class="nav-item px-3">
            <NavLink class="nav-link" href="epic">
                <span class="oi oi-list-rich" aria-hidden="true"></span> Epic
            </NavLink>
        </div>
         <div class="nav-item px-3">
            <NavLink class="nav-link" href="mars-rover-photos">
                <span class="oi oi-list-rich" aria-hidden="true"></span> Mars Rover Photos
            </NavLink>
        </div>
        <div class="nav-item px-3">
            <NavLink class="nav-link" href="nasa-image-and-video-library">
                <span class="oi oi-list-rich" aria-hidden="true"></span> NASA Image and Video Library
            </NavLink>
        </div>
    </nav>
</div>

@code {
    private bool collapseNavMenu = true;

    private string? NavMenuCssClass => collapseNavMenu ? "collapse" : null;

    private void ToggleNavMenu()
    {
        collapseNavMenu = !collapseNavMenu;
    }
}
